import "../globals.css";

export const metadata = {
  title: "Browse Free Content — LofiBuddha",
};

export default function BrowseLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
